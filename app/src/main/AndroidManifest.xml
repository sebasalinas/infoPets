<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="egt.infopets">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".firstScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".mantenedor.addPet"
            android:parentActivityName=".firstScreen" />
        <activity
            android:name=".searchPet"
            android:parentActivityName=".firstScreen" />
        <activity android:name=".mantenedor.editPet"
            android:parentActivityName=".firstScreen"/>
        <activity android:name=".mantenedor.mantenedorSpeciesAndRace"
            android:parentActivityName=".firstScreen"></activity>
    </application>

</manifest>