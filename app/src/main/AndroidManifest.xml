<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="egt.infopets">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mantenedor.FirstScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER"
                    android:windowSoftInputMode="stateHidden" />
            </intent-filter>
        </activity>
        <activity  android:name=".mantenedor.AddPet"

            android:windowSoftInputMode="stateHidden"

            android:parentActivityName=".mantenedor.FirstScreen" />
        <activity android:name=".mantenedor.AddEspecie"

            android:windowSoftInputMode="stateHidden"

            android:parentActivityName=".mantenedor.FirstScreen" />
        <activity android:name=".mantenedor.SearchPet"

            android:parentActivityName=".mantenedor.FirstScreen"

            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".mantenedor.AddRaza"
            android:parentActivityName=".mantenedor.FirstScreen"

            android:windowSoftInputMode="stateHidden" >
        </activity>
    </application>
</manifest>